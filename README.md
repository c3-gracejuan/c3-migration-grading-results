# C3 Website Migration — Grading Results

Read-only static snapshot of the migration QA grader. Open the GitHub Pages URL
to browse per-page verdicts and click through to the live prod and migrated
pages to compare them side by side.

- **Product pages** (47 priority pages): live prod link + deployed migrated link.
  A few whose migrated deploy currently returns a 5xx are marked on the card.
- **Collection tail** (666 pages): live prod link + migrated staging link.

Migrated links sit behind a Vercel visitor password — ask Grace. No screenshots,
no backend, no credentials. Regenerated from the private grader repo via
`export_static.py`.
