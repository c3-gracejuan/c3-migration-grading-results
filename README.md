# C3 Website Migration — Grading Results

Read-only static snapshot of the migration QA grader. Open `index.html` (or the
GitHub Pages URL) to browse per-page verdicts and click through to the live prod
and migrated pages to compare.

- **Product pages** (the 47 priority pages): live prod link + migrated render screenshot.
- **Collection tail**: live prod + migrated staging links (staging needs the Vercel
  visitor password — ask Grace).

No backend, no credentials. Regenerated from the private grader repo via
`export_static.py`.
